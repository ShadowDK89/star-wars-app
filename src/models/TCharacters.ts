export type TCharacters = {
  id: string;
  name: string;
  species: string;
};

export type TSingleCharacter = {
  id: string;
  name: string;
  height: string;
  hairColor: string;
  skinColor: string;
  eyeColor: string;
  birthYear: string;
  gender: string;
  species: string;
};
